#include "../header/modelFunctions.h"
#include <cmath>
// Defining the differential equation to be solved


// diff. equation is: dy/dt = t*R1
float i1alpha(float t, float out, motorParametersStruct motorParameters)
{
 return (0);
}

// udělám to jinak, předpočítám si jednotlivé parametry, které jsou v rovnici podobné a pak je vložím do té rovnice, abych nemusel parametry počítat pořád
// ale někdy i v jednom členu něco počítám dvakrát, tak otázka, jak moc je náročné čtení oproti těm výpočtům

